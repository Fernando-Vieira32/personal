#!/bin/bash
set -e

echo "Running rubocop..."
bundle exec rubocop

echo "Running reek..."
if bundle exec reek --no-color --single-line; then
  echo "Reek passed with no issues."
  exit 1
else
  echo "Reek found code smells."
  exit 1
fi